a= 1;
b= 1;
A= [a; b];

x= [0.10, 0.50, 1.0,  1.5,  2.0,  2.5];
y= [0.10, 0.28, 0.40, 0.40, 0.37, 0.32];

for i= 1:4
R= (y-(x./(A(1)+A(2)*(x.^2)))).';

R_a= x./((A(1)+A(2)*(x.^2)).^2);
R_b= (x.^3)./((A(1)+A(2)*(x.^2)).^2);

J_T= [R_a; R_b];
J= J_T.';

A= A-((inv(J_T*J))* (J_T*R));
disp(A);
end